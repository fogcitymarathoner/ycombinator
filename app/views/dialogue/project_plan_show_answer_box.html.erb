<% form_for @dialogue, :url => {:action => :save_reply, :controller => :dialogue} do |f| -%>
  <%= f.text_area :body, :cols => 60, :rows => 5 %> <!-- TODO: Style in CSS, not here! -->
  <%= f.hidden_field :parent_id %>
  <%= f.submit "Save" %>
<% end -%>

