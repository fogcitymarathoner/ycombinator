$('MB_content').update("<%= escape_javascript(render :partial => 'signupform') %>");
Modalbox.resizeToContent();
$('new_user_session').observe('submit', function(event) {
	event.stop();
	this.request();
});