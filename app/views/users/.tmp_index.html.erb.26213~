<h1>Listing messages</h1>

<table>
  <tr>
    <th>Title</th>
    <th>Body</th>
    <th>Recipient</th>
    <th>Sender</th>
    <th>Created at</th>
    <th>Deleted at</th>
    <th>Read at</th>
  </tr>

<% @users.each do |user| %>
  <tr>
    <td><%=h user.id %></td>
    <td><%=h user.sender_id %></td>
    <td><%=h user.recipient_id %></td>
    <td><%= link_to 'Show', user %></td>
    <td><%= link_to 'Edit', edit_user_path(user) %></td>
    <td><%= link_to 'Destroy', user, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New user', new_user_path %>